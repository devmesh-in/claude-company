# WORRIES - the ledger of the un-acted-on

_Things observed but not yet run down: integration traps, flake sources, spec
ambiguities, anything that produced a surprise or a false-green. One row per
worry, brutally terse. Add a row the moment you notice it. Read this at the
start of every integration or audit step. A row graduates OUT when it becomes a
filed CR, a STATUS risk, or a verified fix - delete it then. P0 blocks a
wave/merge; P3 is polish._

| P | Worry | What (one line) | CEO logic (one line) |
|---|---|---|---|
| P2 | "integrate per-lane, never barrier-wait" collides with branch protection's up-to-date rule | Hit on 2026-08-13 merging #107: CI 9/9 green, but the merge was refused with `mergeStateStatus=BEHIND` because #106 had landed on main after that branch rebased. Fixing it needs `gh pr update-branch` plus a FULL CI cycle (~6 min) before the merge is allowed | The doctrine this program is porting says integrate each lane as it goes green rather than barrier-waiting the wave. That is right for BUILD time and it has a hidden cost at INTEGRATION time nobody costed: with N lanes green at once, every merge puts the other N-1 behind, so merges serialize and each pays an update plus a full matrix. Per-lane integration is therefore O(N) CI cycles, not O(1). Not a reason to go back to barrier-waiting - the build parallelism is where the real time is - but the doctrine should SAY it, so a CEO batching three green lanes expects three cycles instead of being surprised. Options if it ever bites harder: merge several green lanes in one update-and-merge pass, or relax the up-to-date requirement for lanes whose ownership diffs are provably disjoint (which this program's directory-disjointness makes checkable). Fold the honest version into the parallel-discipline section L6 is writing |
| P1 | nothing checks that the hooks PARSE, and an unparseable hook silently stops enforcing | CI runs `node --check` on the two JS files and syntax-checks NONE of the 18 Python enforcement hooks (I said 19; the lane building the gate counted 18 from the directory and flagged the discrepancy rather than adopting my number - correct, and exactly the reason the gate derives its list instead of hard-coding one). Confirmed empirically 2026-08-13: appended `def broken(:` to a copy of `guard_frozen.py`, fed it an Edit targeting a frozen path, got EXIT=1. Only exit 2 blocks, so the edit goes through with no signal in the session that a guard just stopped existing | Found because the L6 lead REPORTED ITS OWN NEAR-MISS - it broke a docstring while editing stop_gate, caught it with an ast.parse check before running anything, and wrote it up instead of quietly fixing it. Same shape as the guard_frozen P0: enforcement silently absent rather than visibly broken, and arguably worse because one stray character disarms a whole guard. Assigned to L6 as the third member of the family it has already built - the wiring gate asserts a hook is WIRED, the canon job asserts doctrine matches reality, this asserts a hook is LOADABLE. Must derive the file list from the directory, not hard-code it, or it becomes one more copy of canon that drifts. Must ast.parse rather than import, since several hooks read stdin at module level |
| P2 | tests that fail in the HARNESS rather than in the product - three instances in one day | (1) `test_tui.sh` permanently 20/1 in any wired checkout, fixed in #105; (2) `pipefail` plus `printf \| grep -q` reporting red on an EPIPE while grep actually matched, twelve sites across two CI suites, unfixed; (3) `test_state_kernel.py:534` asserting a WALL-CLOCK bound (`assertLess(elapsed, 0.5)`, measured 0.5547s) on a loaded macOS runner, failing 1 of 9 runners on PR #107 while the product behaved correctly. FIXED at the mechanism: the retry budget is now asserted by counting `read_json_file` calls (exactly 4) and capturing `time.sleep` (exactly `[0.06, 0.06, 0.06]`, the OQ-HP-10 fallback). (4) A FOURTH instance the same lane found while mutation-testing the first: a test asserting a real git index exists, which fails in a non-git export. So 1, 3 and 4 are fixed; 2 (pipefail) remains | Individually trivial, as a class corrosive out of proportion to their size: each one teaches everyone to RE-RUN a red instead of READING it, and that habit is what lets a real red slide past. The instrument being wrong is not a lesser defect than the code being wrong when the instrument is what everyone trusts. Standing rule from today: a test asserting a wall-clock bound on shared CI is making a claim about the MACHINE, not the product - assert the mechanism (retry count, sleep budget, call count) so the claim holds identically on a fast laptop and a saturated runner. Raising the threshold only moves the flake somewhere less reproducible |
| P2 | `pipefail` plus `printf \| grep -q` is a latent FALSE-RED in two CI suites | Found by the L2 lead 2026-08-13 after `test_update.sh` reported 138/1 on one run and 139/0 on the next with zero code change. The failing line logs `printf: write error: Broken pipe` immediately above the FAIL: under `set -uo pipefail`, an EPIPE on the WRITER fails the whole pipeline even though grep matched. Reproduced deterministically as a mechanism (200/200 pipelines reported failure while grep matched every time), though the lane could not reproduce the specific trigger and said so rather than inventing a load theory. Nine sites in `tests/install/test_update.sh`, three in `tests/cli/test_cli.sh` | The nasty property is that it fails in the HARNESS, so the failure text names a passing assertion - a lane burns a full re-run just to learn the red was fake, and the honest response to a red suite (investigate) is exactly the wasted effort. That is a trust-corroding failure mode: it teaches people to re-run reds instead of reading them, which is the same habit the permanently-red TUI suite was teaching before today's fix. One-character class of fix (`grep -q ... <<< "$OUT"`, or `grep -c` without `-q`). Owned by no lane this wave; candidate quick task once wave 2 clears |
| P0 | guard_frozen is INERT inside every worktree - frozen surfaces are unenforced exactly where all delegated work happens | Raised by the L1 lead 2026-08-13, then reproduced by the CEO with a control: the IDENTICAL Edit to the IDENTICAL frozen file exits 2 (BLOCKED) in the main checkout and exits 0 (ALLOWED) at `.claude/worktrees/<slug>/company/state/gates.status`. Mechanism: worktrees live INSIDE the project root, so `_common.rel_path` returns `.claude/worktrees/<slug>/company/state/gates.status` - inside the tree but PREFIXED - and that string matches no frozen pattern. CORRECTED 2026-08-13 by the lane that fixed it, and the correction matters: NOT everything keyed on rel_path was affected. `guard_tests` already held, because it keys on path SEGMENTS (`tests` appearing anywhere) rather than an anchored pattern, so only 5 of the 13 new tests fail against the old code. My original "everything keyed on rel_path is affected" was too broad. The asymmetry is precisely why the hole stayed invisible: guards consume rel_path differently, so a spot-check of one guard would have concluded enforcement was fine. Confirmed affected: the frozen registry, the always-list, and accepted-ADR immutability | This is the most serious defect this program has surfaced and it is NOT a ported change - it is pre-existing, and the port only found it because a lead was reading that code. METHOD mechanism 3 says frozen surfaces change only by change request and "the bureaucracy is the point"; that bureaucracy has been enforced only in the main checkout, which is where the CEO works, and NOT where builders work. Every delegated builder in this repo's history could have patched any frozen surface locally. What held instead was doctrine: L3 filed CR-2 and L1 filed CR-HP-1 because the agents follow the brief, not because a hook stopped them. Honest reading - the social layer worked and the mechanical layer was absent, which is exactly the arrangement this company exists to avoid. Assigned to L1 (owns `_common.py`, has the context, already finished its main pass). Fix strips the worktree prefix before matching or resolves against the containing work tree; every guard using rel_path needs a test |
| P2 | nothing checks that CLAUDE.md's named suites match what CI runs | CLAUDE.md said "the two suites that actually gate THIS repo" and annotated npm test as covering install. Both false: npm test is tests/cli/test_cli.sh alone, and CI runs five suites plus three scans. Text corrected 2026-08-13, but nothing mechanical keeps it true | THIRD instance of this family after W-030 (run_tests.sh not reachable from CI) and the test_update.sh gap - the first two were tests CI could not see, this is the mirror image, suites CI runs that canon does not name. It cost real work: the error propagated into three sealed briefs, two lanes and my own verification all ran two suites and called it done, and CI then caught 13 failures in a suite none of us ran. Candidate fix, and it is the same shape as the wiring gate this program is already building: a CI step that parses the suite commands out of ci.yml and fails when CLAUDE.md does not name every one of them. Cheap, and it makes canon drift a red gate instead of a briefing error |
| P1 | a worktree lead's green commit gate proves nothing about its worktree | guard_commit's stamp check resolves its root from CLAUDE_PROJECT_DIR, so it reads the MAIN checkout's gates.status. Found by the L2 lead 2026-08-13: its commits passed the gate because the main checkout had been stamped green at 05:01:27Z - BY ME, running the ladder to clear an unrelated stop_gate block. No BYPASS was logged; the gate was genuinely satisfied, just by evidence about a different tree | Note the causal chain, because it is the whole lesson: the stop_gate scoping defect forced a ladder run on main, and that green stamp then silently satisfied the commit gate for two lanes whose code was never in that tree. One defect manufactured the evidence that hid another. L2 deliberately did NOT redirect the stamp check (its deviation 2) and was right to: today a worktree cannot PRODUCE a stamp, so redirecting it would false-block every worktree commit. That changes once L3's FR-HP-28 lands and a worktree ladder stamps its own tree - at which point redirecting the commit-time stamp check becomes both possible and correct. Wave-2 candidate, sequenced after L3 merges, and it belongs with L5 since mode C has the same root-scoping problem |
| P1 | CR-UPD-1 half-landed one release ago | company/state/install-manifest.json and company/state/.update-backups/** are in the frozen-surfaces.json always list but MISSING from guard_frozen.ALWAYS_DEFAULTS. Verified at base commit 55cf436, so it predates this program. install.sh copies the registry with copy_if_absent and update.sh restores it only when absent, so the JSON reaches FRESH installs only - both paths are unprotected on every existing install | Raised by the L3 lead against its own FR-HP-23, which is built on exactly this reasoning and lands both spots correctly. Same defect class as the pack-state-leak and the CI-reachability rows: a change that needs two landing spots got one, and nothing mechanical noticed. Owed its own CR and a one-line fix; deliberately NOT folded into L3 because it is outside that lane's FRs and its brief grants it only CR-2. Candidate: the wiring-gate idea generalizes here - assert the registry always-list and ALWAYS_DEFAULTS agree, so this class cannot recur |
| P1 | stop_gate cross-blocks concurrent sessions | It reads EVERY active entry but checks ONE global stamp, so with N sessions any session's stale tree blocks every other session at every turn end. Reproduced 2026-08-13 with two feature entries and a stale stamp: it blocks and names both slugs, and the recipe offers only "run the gates" (which means absorbing another session's in-flight work) or "close YOUR entry" (premature). | Shipped in v0.2.6 alongside multi-session-tasks, which is what made concurrent entries normal - the gate was written under the single-session assumption and nobody re-derived it. DevMesh hit this and their answer was to un-wire stop_gate entirely; that is wrong for us, because stop_gate is the ONLY check on three paths guard_commit cannot see (edit-and-never-commit, commit-green-then-edit-more, and closing an entry with red gates). Fix is scoping, not deletion - assigned to L6 (#102), which must pick between attributing the block to this session's own work or blocking only when a single gating entry is active. HIT LIVE THE SAME DAY, against the CEO session that logged it: with hp-kernel, hp-guards and hp-runner active, the Stop hook blocked the CEO and named all three, even though every one of those lanes builds in its OWN worktree and none of their code is in the main checkout. The only honest resolution was a full ladder on a tree containing nothing but paperwork - briefs, the spec, state files - which cost 459s (hooks 55s, npm 404s). That is the real price tag of the defect, and it is also the program's measured "before" number for the observability work. |
| P1 | dispatched worktree agents cannot commit at all | guard_commit.git_cwd prefers payload cwd, which the harness pins to the MAIN checkout; a developer in .claude/worktrees/<slug> on task/<slug> is judged to be on main and blocked as "commit on protected branch". Reproduced 2026-07-26 on task/ci-test-discovery; the agent correctly refused to evade and left its work staged | The #26 git_cwd fix only helps when the session cwd really is the worktree, which is never true for a dispatched subagent. Today's workaround is the CEO committing from the main checkout on the task branch, which converts every delegated build into self-authored work needing an audit - i.e. it silently defeats the delegation economics. Candidate fix: resolve the branch from the edited file's worktree, or accept a worktree path as proof of branch. Fold into the multi-session engagement |
| P2 | company/ still ships files nothing installs | After pack-state-leak, the tarball still carries company/witnesses.json, company/adr/**, and company/RELEASE.md - none appears in install.sh, update.sh, or lib/payload_paths.sh, so nothing ever copies them into a target. witnesses.json is this repo's own registry, tagged with our task slugs and code substrings | Same defect class as issue-68 and pack-state-leak, but lower impact: the hooks those witnesses reference ship anyway, so it is dead weight rather than disclosure. Auditor finding F1 on pack-state-leak. Candidate: negate all three, or decide deliberately that adr/RELEASE are shipped documentation and only negate witnesses.json |
| P2 | test_tui can never be green in a wired checkout | tests/install/test_tui.sh asserts an install leaves CONFIGURE ME placeholders, but the installer copies the WORKING-TREE gates.config, which on this repo carries real gate commands. So the suite is 20/1 locally and 21/0 only on a clean copy - the failure looks like a regression every time | Confirmed twice on 2026-07-26 (once mistaken for a pack-fix regression, then proven innocent on an index-only copy). Root cause is the same dual-nature tension as the existing gates.config row. Candidate: have the test install from `git archive HEAD` rather than the working tree |
| P2 | CI npm may hide what local npm catches | tests/cli/test_cli.sh pack-manifest assertions were fully dead locally on npm 10.5.0 (prepare banner on stdout corrupts the JSON) while CI stayed green - 10 assertions were failing on main and CI never said so | Fixed via --silent in test-infra-fixes; the general lesson is that a suite passing in CI is not evidence it passes for a contributor. Candidate: pin or report the npm version in the CI log |
| P2 | gates.config template-vs-local tension | On the product repo itself, tracked gates.config must stay placeholder (ships to installs) so stop_gate/guard_commit need uncommitted local wiring - a papercut every session | Consider gates.local.config override in run-gates.sh; candidate issue after wave 2 |
| P3 | costs.log accuracy limits | Multi-model stop intervals book tokens to the last model; sub-half-cent stops log est=$0.00 | Known limits from wave-1 report; revisit if spend reporting starts driving decisions |
| P2 | staging stales a provenance audit | work_hash includes diff --cached, so git add AFTER the auditor pass stales the audit even with identical content | Auditor worry #2: doc the order (stage, then audit, then commit) in ORCHESTRATOR step 7 wording next doctrine touch; operational for now |
| P3 | git -C evades guard_commit subcmd parse | guard_commit.git_subcmd treats -C <dir> as the subcommand token, so `git -C x commit` bypasses commit gates (pre-existing, Mode C inherits) | Anti-adversary, out of doctrine scope; candidate issue against guard_commit |
| P3 | Mode D lacks worktree-cwd exemption | Stop close-gate block message reads wrong if a worktree session ever gets an active-task.json (moot today - file is untracked) | Auditor worry #3; per-spec (mirrors stop_gate); revisit only if worktree sessions gain task state |
| P2 | session_start 60-line cap can hide digest | Saturated RESUME+STATUS could truncate the active-task + execution digest lines | Lead worry P2, inherited behavior; candidate tail-reservation tweak in session_start |
| P1 | provenance audit verdict parser is substring-naive | guard_provenance.py:487 records the negative verdict if that literal phrase appears ANYWHERE in the audit payload - including the CEO's own dispatch prompt. Hit 3x on release-0.2.2, then a 4th time on test-infra-fixes where it BLOCKED the commit twice against two passing audits | Raised to P1: it now costs real dispatches, and the trap is near-inevitable because naming the verdict vocabulary is the natural way to prompt an auditor. PROVEN WORKAROUND: give the auditor a vocabulary that cannot poison the record (SHIP / SHIP-WITH-FIXES / HALT) and forbid it from emitting the negative token. Real fix: anchor the parse to the response's verdict line. Fold the vocabulary change into .claude/agents/auditor.md so no CEO has to remember it |
| P2 | SendMessage-resumed audits record no provenance | Only Task/Agent spawns fire the PostToolUse event that writes an audit into the ledger. Continuing an auditor via SendMessage produces a real audit that the ledger never sees, so the commit stays blocked and the reason looks wrong | Found on test-infra-fixes after two resumed rounds counted for nothing. Doctrine fix is cheap: a re-audit must be a fresh dispatch, never a resume. Candidate: say so in ORCHESTRATOR step 7 |
| P2 | guard_tests resolves test_scope from main checkout | Worktree builders cannot commit brief-granted test edits until the CEO sets test_scope in MAIN's active-task.json - has now blocked two workstreams (cli-update, model-routing-arming) | Candidate fix: resolve test_scope from the target worktree, or dispatch protocol sets it whenever a brief grants test dirs; follow-up brief |
| P3 | models.json merge is now a THIRD duplicated heredoc | install.sh/update.sh byte-identical coupling grew (settings, CLAUDE block, now models.json builtins) - divergence risk compounds | Same root cause as existing heredoc row; the byte-identity witness candidate (lead proposal 3) mitigates; fold into any future extraction task |
| P2 | update.sh duplicates install.sh merge heredocs | The three merges (settings/mcp/CLAUDE block) + CC_BLOCK are copied verbatim; a future install.sh merge change silently diverges update | cli-update lead worry; candidate fixture test asserting install and update produce identical merges; extraction forbidden this pass by additive-only brief |
| P3 | update spawns python3 per file (~3x/file) | ~180 spawns for 61 files - seconds today, slow at scale | Candidate manifest.py hashtree batch subcommand; not blocking |
| P3 | manifest emission fails open at install | lib/ or package.json missing -> no manifest, update runs bootstrap-safe forever (never clean "updated") | By design (safe); revisit only if field reports show chronic manifest-less installs |
| P3 | extensionless files count as source | guard_spec.is_source flags any extensionless file outside company/.claude/docs/.github (e.g. Makefile, a bare "report") - a non-code agent writing one hits the execution gate | Found in owner-requested gate probe 2026-07-15; md/json/state are proven exempt; candidate: extensionless allowlist if it ever bites |
| P3 | guard_tests gates md inside tests/ | Any write under a tests/ segment is blocked without test_scope regardless of extension, incl. tests/**/*.md (docs-librarian MODULE.md case) | Defensible (tests are the oracle) but extension-blind; candidate: exempt NON_SOURCE_EXT under tests/ if doc syncs start colliding |
| P2 | RISK-MST-01 ANY-hotfix bypass | multi-session-tasks: with entries [feature-a, hotfix-b] active, guard_models, guard_commit, provenance Mode C and provenance Mode E are all bypassed where [feature-a] alone would arm them, so unrelated feature work rides the emergency waiver | ACCEPTED deliberately: blocking a declared production emergency behind an unrelated entry is the worse failure. Mitigations: every such bypass writes an adherence.log BYPASS line naming the responsible hotfix entry; the context pin and the session digest both render a loud HOTFIX marker; METHOD.md makes removing the hotfix entry part of the already-mandatory postmortem. Escalation: narrow the waiver to the hotfix entry's own paths |
| P2 | RISK-MST-02 ANY test_scope | multi-session-tasks: a blanket "test_scope": true on entry B unlocks test-file edits for entry A | ACCEPTED: glob-scoped grants were scoped out by the owner. Mitigation: an adherence.log GRANT line naming the granting entry whenever more than one entry is active. Escalation: test_scope becomes a glob list |
| P3 | RISK-MST-03 ledger resurrection | multi-session-tasks: remove entry A and re-add it before the next write_ledger prunes its record, and the new incarnation inherits the old dispatches | ACCEPTED: narrow window, and the incarnation check was scoped out by the owner. Escalation: stamp each entry with an incarnation id at add time and key the ledger on it |
| P2 | RISK-MST-04 nothing enforces Edit-over-Write | multi-session-tasks: an agent can still Write the whole active-task.json and silently drop a sibling session's entry | Doctrine is the ONLY control, which is why the FR-MST-24 wording carries real load. Escalation named and costed: a guard_frozen rule that blocks Write on active-task.json while still allowing Edit, roughly eight lines, deliberately NOT built this pass |
| P3 | RISK-MST-05 a prefix slug steals a sibling's dispatch | multi-session-tasks: OQ-MST-04 attribution is a case-sensitive SUBSTRING match, so entries `feat` and `feat-a` with ONE dispatch whose prompt reads "build task/feat-a per brief" credit BOTH, and Mode E then returns 0 for both - exactly the vacuous satisfaction FR-MST-22 step 7 exists to prevent. Verified 2026-07-27 | Not a regression versus today (at one entry a dispatch is attributed unconditionally anyway) and it is the authorized fallback, but it is the one N > 1 residual that had no row. Mitigation is doctrine only: slugs are unique per working tree AND no slug may be a prefix of another (stated in METHOD.md beside the entry shape). Escalation: match a word boundary or the exact `task/<slug>` token instead of a bare substring |
| P3 | background writers stale the gate stamp | Field report 2026-07-15: a training job writing untracked non-ignored files (data/) stales work_hash every turn - stop_gate demands gate reruns | By design (.gitignore IS the ignore mechanism); candidate one-line docs FAQ: gitignore background-process output dirs |
| P2 | a clean delegated build gets NO independent audit | The mandatory-audit rule is triggered by SELF-AUTHORSHIP, not by risk. multi-session-tasks was a 4791-line feature touching every enforcement hook, and because the lead committed cleanly from its worktree the ledger recorded self_authored=0, so guard_provenance Mode C never armed and no auditor ever read the diff. The dispatch plan had assumed the opposite and pre-committed to an audit that then never happened | Found at the 2026-07-29 close-out. The hierarchy is supposed to substitute for the audit (developer reports, lead verifies, CEO judges), but nothing mechanically checks the CEO actually judged. Candidate fix: make risk_score's `high` band arm a provenance requirement the same way self-authorship does, so a large delegated diff cannot integrate unaudited either. Until then the CEO must dispatch the auditor deliberately on any large delegated merge - a decision, not a reflex |
| P3 | a new test FILE is not automatically reachable from CI | tests/install/test_update.sh (139 cases, incl. the entire FR-MST-29 field-rollout proof) ran only via package.json prepublishOnly - never on a PR - because ci.yml enumerates suite files by hand and nobody added it. Second instance of this exact class after W-030 | Fixed for this file in the v0.2.6 close-out by adding an explicit step to ci.yml and release.yml. The class is not fixed: the next test file added beside an existing runner will be just as invisible. Candidate: a CI step that globs tests/**/test_*.sh and fails on any file no workflow step names |
| P1 | provenance dirty check is umbrella-scoped, not repo-scoped | guard_provenance.dirty_source_paths runs `git -C <project_root> status --porcelain -uall -- . :(exclude)company/state`, and project_root is CLAUDE_PROJECT_DIR - the UMBRELLA root in a polyrepo install. The check therefore counts dirty source anywhere under the umbrella as this session's unaudited work. TWO call sites share the identical function: :832 (Mode C, commit) and :878 (Mode D, Stop). Reported against the commit gate on the umbrella project's board as P0, but it is wider than commits: a clean, fully-delegated session cannot reach Stop without either faking an audit or deleting files belonging to another session | Recorded HERE for the first time on 2026-07-29 - the defect was only ever logged on the consuming project's board while the defective code lives in this repo, which is how it stayed unfixed. NOT folded into v0.2.6 on purpose: narrowing what counts as dirty converts BLOCKs into ALLOWs, the exact monotonicity class multi-session-tasks was built to protect, so it needs a spec and an independent read rather than a release-time patch. The design question to answer first is what the scope SHOULD be - the git repo containing the edited file, the nearest .git above it, or the paths the session actually touched (that last one needs session-keyed state, which the multi-session spec deliberately scoped out). Candidate for 0.2.7 as its own feature |
