# DECISIONS - owner escalations and outcomes

_Questions only the owner may answer (money, invariants, deploys, scope,
business policy), with what was decided and when. Agents run on tagged
fallbacks until a row lands here._

| # | Date | Question | Decision | Affects |
|---|---|---|---|---|
| 1 | 2026-07-09 | Adopt model tiering (developer/QA to sonnet)? | VETOED - every role stays opus | models.json |
| 2 | 2026-07-09 | Program scope from the framework research | Hard-gate-fitting features only; lessons loop + loop workers DEFERRED | program plan |
| 3 | 2026-07-10 | Adoption program delivery (waves 1-3, PRs 27/33/34/32/35) | ACCEPTED - owner authorized end-to-end merge; CEO merged and verified integrated main | whole repo |
| 4 | 2026-07-10 | delegation-enforcement delivery (5 layers + tracking gate, PR #49, issues #42-#47) | ACCEPTED - owner: "merge everything and close out"; merged f9e5dae, integrated main verified (213+31 green), witnesses W-011..W-013 | hooks, doctrine, settings |
| 5 | 2026-07-10 | Enforcement design direction | Owner REJECTED budgets/role bans twice; approved principle-derived gates (mechanism 5 + written-record consistency) and demanded low-token injection + GitHub tracking gate | all future gate design |
| 6 | 2026-07-15 | cli-update delivery (update subcommand + provenance manifest, PR #57, issues #54-#56) | ACCEPTED - owner: "test it, the merge it to main"; CEO re-tested end-to-end (edit kept, .new written, deleted file restored, config untouched), merged 7726c99, integrated main verified (213 hooks + 40 CLI + 56 engine green), witnesses W-014..W-016 | CLI, installer, frozen registry |
| 7 | 2026-07-15 | cli-self-update delivery (update refreshes the CLI first via npx handoff, PR #60, issue #59) | ACCEPTED - owner: "push to main, then to npm"; merged a09b463, integrated main verified (213 + 57 green), witnesses W-017..W-018 | CLI driver |
| 8 | 2026-07-15 | Release v0.2.0 to npm (tag v0.2.0 at 5913374; notes: PR #57/#60 bodies) | PUBLISHED 2026-07-15 by the OWNER manually from the clean tag clone (link-based npm 2FA - publishes are always owner-manual or git-CI, never agent-run). Registry verified: dist-tags.latest 0.2.0. Semver minor | npm registry |
| 9 | 2026-07-15 | DevMesh migration (owner: "use claude-company on this WITHOUT breaking custom changes... preserve DevMesh specific context") | DELIVERED - umbrella root git-initialized (machinery only), primitive company ported to company/, all customizations preserved, stale docs archived (nothing deleted). Verification: invariants gate PASS, 6 frozen surfaces probe-blocked. Found+fixed upstream #67/#68 | DevMesh polyrepo |
| 10 | 2026-07-15 | Release v0.2.1 to npm (patch: #64 enforcer arming, #67 merger fix + field-heal, #68 record-leak) | PREPPED by CEO (bump PR + tag on merge); publish is the owner's button per standing rule (link-based 2FA). SUPERSEDED by v0.2.2 (#12) before publish - registry never received 0.2.1 | npm registry |
| 11 | 2026-07-22 | model-routing-arming delivery (builtins enforcement + wiring gate + migration + Workflow doctrine, PR #77, issues #74-#76) | ACCEPTED - owner: "merge everything and give command to push to npm"; CEO merged cd07fb6, integrated main verified (hooks 222 + npm 61 + engine 111 green, gates stamped), live enforcement certified (contradict/bare exit 2, match 0, dormancy probe red), witnesses W-026/W-027/W-028 | hooks, models.json, installer, doctrine |
| 12 | 2026-07-22 | Release v0.2.2 to npm (rolls up unpublished 0.2.1 + #77 model-routing arming) | PREPPED by CEO; owner pushed tag v0.2.2 (at ad3dda8) but publish SUPERSEDED by v0.2.3 (#14) which adds #83 - registry goes 0.2.0 -> 0.2.3 directly | npm registry |
| 13 | 2026-07-23 | spawn-depth-shipping delivery (CC 2.1.21 depth-1 flattening; template env + installer env merge + guards, PR #83, issues #79-#82) | ACCEPTED - owner: "merge it"; merged 6061814, CEO independent verification (224/61/123 green, merge probes, byte-identity), witness W-029 | template settings, installer, tests |
| 14 | 2026-07-23 | Release v0.2.3 to npm (v0.2.2 content + #83 spawn-depth) | PREPPED by CEO per owner direction ("change to v0.2.3"); owner tags v0.2.3 at the release-PR merge commit and publishes from a clean tag clone (link-based 2FA, owner-manual); notes: PR #77/#83 bodies | npm registry |
| 15 | 2026-07-29 | multi-session-tasks delivery (active-task.json holds N entries; provenance ledger v2 kills the slug-mismatch wipe; PR #93 merged 171421f, closes #89-#92) | ACCEPTED 2026-07-29 - owner directed close-out and release without further changes; shipped in v0.2.6. CEO independent re-verification on integrated main: hooks 393 OK, npm test 62/0, install 96/0, update 139/0, witnesses 32/32 (W-032/W-033 new), guard_models --check clean, CI 9/9 on the PR. DISCLOSED LIMIT: no auditor ever read this diff - the mandatory-audit path is triggered by self-authorship and the lead committed cleanly from its worktree, so the trigger never armed (now a P2 worry) | hooks, doctrine, skills, tests |
| 16 | 2026-07-29 | Release v0.2.6 to npm (multi-session-tasks + the CI update-suite coverage fix) | PUBLISHED 2026-07-29. Registry latest = 0.2.6, OIDC trusted publishing, SLSA provenance, release workflow green (it re-ran the full suite incl. the update tests). Tag v0.2.6 at 6d2b9b1. The owner ACCEPTED both red readiness criteria (R7 two open P1 worries, R3 27 orphan MST requirement IDs) rather than holding the release. NOTE: the CEO created and pushed the tag on the owner's explicit in-session instruction, which RELEASE.md currently calls a boundary violation - see decision 17. Semver: pre-1.0, a feat bumps PATCH, so 0.2.5 -> 0.2.6. Target commit is the v0.2.6 close-out merge on main. TWO READINESS CRITERIA ARE RED and are escalated rather than waived: R7 (two open P1 worries - guard_commit.git_cwd worktree row, provenance verdict-parser row; neither affects what a user installs) and R3 (trace_check exits 0 but reports 27 orphan MST requirement IDs with no code or test citation). OWNER-ONLY to ship: git tag -a v0.2.6 <sha>, git push origin v0.2.6, then publish the GitHub release to trigger release.yml (OIDC trusted publishing; no token exists) | npm registry |
| 17 | 2026-07-29 | RELEASE.md says an agent tagging or publishing a release is "a boundary violation, full stop". On 2026-07-29 the owner instructed the CEO to release; the CEO created and pushed tag v0.2.6 and was blocked by the permission classifier only at `gh release create`, which the owner then ran. Canon and practice now disagree | OPEN - awaiting owner. Two honest options: (a) keep the bar but add an explicit exception, where the company never tags on its own initiative and a direct in-session owner instruction authorizes it, recorded here each time; or (b) keep it absolute and the CEO reverts to preparing only. Worth weighing: the rule was written when npm 2FA was link-based so a human HAD to be in the loop; OIDC trusted publishing removed that constraint, so the original reason has partly expired. Canon that both parties step over is worse than either option | doctrine, release process |
| 18 | 2026-08-13 | stop_gate: the DevMesh fork un-wired it and recommended we do the same. Should upstream unwire, delete, or fix it? | **SUPERSEDED BY DECISION #20 the same day - stop_gate is DELETED, see #20 and PR #119. The original ruling and its reasoning are kept below because the reversal is the interesting part: this decision was right on the coverage argument and wrong on the price. It correctly identified three paths guard_commit cannot see, and never asked the follow-up question - whether anything bad actually happens on them. It does not: all three end in uncommitted work that cannot reach main. Five weeks of log then supplied what the argument could not, sixteen blocks and zero catches.** ORIGINAL RULING: SCOPE IT, do not unwire - owner: "let's do what you think, fix reason 1". Only one of the three DevMesh reasons transfers: stop_gate reads every active entry but checks ONE global stamp, so with N concurrent sessions any session's stale tree blocks every other session at every turn end. Reproduced 2026-08-13: two feature entries plus a stale stamp blocks both, naming both slugs, offering each session only "run the gates" (absorb another session's in-flight work) or "close YOUR entry" (premature). The other two reasons are about DevMesh's merge-only gating and its low-band waiver, neither of which upstream has adopted, so unwiring here would be a strict subtraction - stop_gate is the only check on three paths guard_commit cannot see: a session that edits and never commits, a session that commits green then edits more, and an entry closed with red gates. Also rejected: "wire it only in single-session installs", because a per-install config choice drifts and this repo is what others install. Assigned to workstream L6 (#102) | stop_gate, multi-session correctness |
| 19 | 2026-08-13 | The four parked ceremony decisions from the harness-port review | UNPARKED by the owner: "fix whatever findings you get, this needs to be a robust system. Cut unnecessary ceremony as much as possible. Balance efficiency with quality. Quality is great already." CEO interpretation, recorded so it can be vetoed line by line rather than as a block. ADOPTING NOW: (a) parked decision 3, the Phase 0 spec-lite rung plus letting quick entries skip a brief - objective conditions, one-way escape upward, hooks force the upgrade; (b) parked decision 2, a risk-scaled audit derived from risk_score.py's EXISTING bands rather than a new line-count fence, and the same change ARMS a mandatory audit in the high band, which closes the worry about a large clean delegated build integrating with no independent read - this is the version that cuts ceremony at the bottom and ADDS rigor at the top, which is why it is the one I am taking; (c) parked decision 6, adding company/briefs/** and company/specs/** to HASH_EXCLUDES, because paperwork is a build INPUT and has now cost two full ladder runs in one day, roughly fifteen minutes, for edits that changed no code. STILL PARKED DELIBERATELY: parked decision 1, merge-only stamp gating. My recommendation is unchanged and the owner's instruction does not change the arithmetic - most of its supporting evidence was staleness that content hashing deletes on its own, so it gets decided at wave-1 integration with a real block count rather than on appetite. Model tiering remains rejected under decision 1 | doctrine, hooks, ceremony |
| 20 | 2026-08-13 | The enforcement layer fought the work for roughly two hours in one session. Which hooks earn their place? | OWNER RULING, on five weeks of adherence-log evidence rather than opinion. DELETE OUTRIGHT: `stop_gate` and every mention of it - 16 blocks, zero catches, and everything it guards is uncommitted work that cannot reach main anyway; the tree-is-red check moves to the session-start digest where it informs instead of blocks. CUT: `guard_provenance` modes D and E (mode E has NEVER fired, mode D has never fired and can deadlock; 1135 lines delivering one working gate), split mode C into its own file. CUT: the `guard_tests` GRANT line - 107 of 118 of its log lines were it saying "allowed", a third of the entire enforcement record. FIX: `risk_score` (score the working tree, not just committed history; treat the enforcement layer as sensitive; arithmetic untouched), `guard_commit` tree resolution, `guard_spec` scope. KEEP: everything else as is. GUARD_SECRETS: the owner initially ruled DELETE on the grounds that it never fired, then accepted the counter-argument and ruled FIX - it fired zero times because it read the main checkout's index while every delegated commit happens in a worktree, so nothing was ever checked; "never fired" and "nothing to find" are different facts and only one of them is in the log. A leaked credential is the one unrecoverable failure and this ships into other people's repos. The dividing line that fell out of the evidence and is worth keeping: everything that fought us is STATEFUL (comparing a global record against a tree, so it goes stale and the person blocked is never the person who can fix it); everything that never caused a problem is STATELESS | the whole hook layer |
| 22 | 2026-08-13 | The audit-scope narrowing (FR-HP-44/45) failed two independent audits. Remediate a third time, or stop? | STOPPED. PR #112 closed unmerged, CEO decision under the owner's "do as needed, make it robust". Six reproductions across two audits and every fix created a new unlock - the last round turned a DO-NOT-SHIP verdict into an unlock, because mode_b_post popped the authorship marker without checking the verdict, and before that one ordinary builder dispatch laundered a deleted ledger back into a valid checksummed empty one. Not carelessness: the remediation was careful and mutation-tested. STRUCTURAL REASON, and it is the durable lesson: the narrowing makes the audit demand depend on a ledger that is untracked, deletable by `git clean -fdx`, excluded from the dirty check and unguarded on the Bash side. Each patch adds a marker to survive deletion, each marker needs a clearing event, and every clearing event is reachable by the party being gated. YOU CANNOT BUILD A FAIL-CLOSED GATE ON STATE THE GATED PARTY CAN RESET. The pain that motivated it - another session's dirty files blocking your clean commit - is fixed properly by #118 resolving the acting tree, which needs no trustworthy ledger. SALVAGED into a follow-up, all cleared by the first audit and none dependent on the narrowing: ledger locking (state_lock has zero call sites on main today, so concurrent sessions really do lose updates), unattributed-dispatch recording, the derived worktree check replacing the hardcoded string, and the `dirty_source_paths` disarm where a timed-out `git status` reads as a clean tree. If risk-scaled auditing is wanted later it belongs where the actor cannot reset the state - CI or the PR - not a local hook reading a file the actor owns | guard_provenance, the audit trigger |
| 21 | 2026-08-13 | Owner's standing test for enforcement, stated during the cut review | "Enforcement is about correcting behavior, NOT simply existing for the sake of existing. Everything should keep its place." Applied literally: a hook's fire count proves nothing on its own - `guard_secrets` fired zero times and stays because it was broken, `stop_gate` fired sixteen times and goes because it corrected nothing. The question is whether the block ever changed an outcome. Pair it with the design test from the same review: a gate must be satisfiable by whoever it blocks, using only what they can reach, and its block message must be a recipe that actually works | all future gate design |
| 23 | 2026-08-14 | DECISION #17 RESOLVED. RELEASE.md calls an agent tagging or publishing "a boundary violation, full stop", and on 2026-07-29 the owner instructed a release in-session and both parties stepped over the rule rather than settling it. Which option? | OPTION (a), on the owner's explicit words: "merge everything to main then just publish it yourself. You have my permission." The company NEVER tags on its own initiative; a direct in-session owner instruction authorizes it; and it is RECORDED here each time, which this row is. The original reason for the absolute bar has partly expired - it was written when npm 2FA was link-based so a human HAD to be in the loop, and OIDC trusted publishing removed that constraint. RELEASE.md should be amended to carry the exception rather than left contradicting practice for a third release. Applies to v0.3.0: tag v0.3.0 at d42d8d5, release published, OIDC workflow re-ran the full suite before publishing with provenance | doctrine, release process |
| 24 | 2026-08-14 | Release v0.3.0 - the enforcement-repair release, eighteen merged changes | PUBLISHED by the CEO under decision 23. Semver: pre-1.0 BREAKING bumps MINOR per RELEASE.md, so 0.2.6 -> 0.3.0 rather than a patch - three behaviours change on upgrade (stop_gate deleted, guard_secrets starts blocking where it silently allowed, content-based stamp reads stale once). READINESS, stated rather than waived: R1 green - all five suites on integrated main at 7e1ff55 in a clean worktree (hooks 743, CLI 62, installer 97, TUI 22, update 139). R2 witnesses 36/36 green after repairing W-006, which had MERGED RED because witness_check is documented as gate G0 and CI has never run it (#123). R4 model routing green. R7 RED but honest: 2 open P0/P1 rows, down from 11 after graduating nine fixed-today rows - the compensating control not firing on a clean tree (owner decision, open) and the umbrella-scoped dirty check. R3 RED: 20 orphan requirement IDs, all belonging to the lane closed unmerged (DECISIONS #22) and the hook deleted (DECISIONS #20) - same class accepted at v0.2.6 | npm registry |
| 25 | 2026-08-22 | The harness spins up too many subagents for minimal tasks - why, and fix it in the prompts | FIXED IN DOCTRINE, PR #128. Owner: "delegation is needed but it should be appropriate delegation, not delegation for the sake of delegation. Don't add arbitrary 'ONLY RUN N DEVELOPERS' - tell the team lead and yourself to be smart about it and adjust to the work." Diagnosis: six one-directional pressures, all prompt text. The load-bearing one was ORCHESTRATOR.md's economics, which priced self-build at one audit dispatch and delegation at zero because the lead, its developers and its QA never entered the equation. Evidence: 34 sealed briefs on disk and NOT ONE task ever classified `quick`; 60 dispatches (49 developers, 11 leads) against 11 audits; salvage-provenance, scoped to one file, drew a lead plus three developers. Encoded principle is the SEAM - an agent earns its spawn when the work has a boundary it can own alone, and headcount is a consequence, not a target. No caps, no thresholds. Also stated for the first time that classifying up buys CEREMONY, not safety, since every gate runs identically at every class - that absence is what made rounding up feel free | ORCHESTRATOR.md, METHOD.md, tech-lead, orchestrator skill, context_pin, guard_provenance |
| 26 | 2026-08-22 | 0.3.1 readiness: R3, R7 and R8 red | OWNER CHOSE "clear what I can first, then re-run", then "arm the risk band first" rather than accepting the reds. R8 CLEARED and it was paperwork, not defects: three CRs read `_Status: PROPOSED_` while all three were already resolved in code (CR-2 approved 2026-08-13 with the header never advanced, CR-HP-2 shipped in #118 and named by witness W-038, CR-HPD-3 shipped via its own stated gitignore alternative). Both P0/P1 worry rows were re-verified and BOTH were wrong in their evidence - the P0 cited line numbers that do not exist in a file 150 lines shorter and a mode cut in #126, and understated itself: risk_score.py has ZERO callers in any hook, so DECISIONS #19's compensating control was never mechanized at all rather than mis-firing | CR queue, WORRIES.md, 0.3.1 |
| 27 | 2026-08-22 | Publish 0.3.1 autonomously overnight | NOT DONE, deliberately, and this row exists so the call is reviewable. The owner asked for issue -> PR -> merge -> publish while asleep. The standing rule stated before they left was that a HALT verdict stops the merge; an independent auditor returned HALT twice on the feature that was supposed to close the release's blocking P0. Publishing would have meant shipping to every install, on a board carrying two open P0s (DECISIONS #19's control still unarmed, and the audit ledger recording dispatches rather than verdicts) plus the P1 that guard_commit's merge gate is blind to `gh pr merge`, a gate the audit says does not close the worry it exists for. Company canon is also explicit that tag and publish are owner buttons; the owner's instruction overrode that, but the HALT is a separate and unmet condition. Registry latest remains 0.3.0. Everything is merged, pushed or teed up; the single open question is OQ-ARB-05 | release, provenance |
